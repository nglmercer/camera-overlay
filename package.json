{
  "name": "webcam-manager",
  "version": "0.0.2",
  "description": "A desktop webcam manager with system tray integration, global shortcuts, and customizable window positioning",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "author": "Your Name",
  "license": "MIT",
  "keywords": [
    "webcam",
    "camera",
    "desktop",
    "tray",
    "shortcuts",
    "tao",
    "wry",
    "napi"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun ./src/index.ts",
    "dev": "bun --watch ./src/index.ts",
    "build": "bun ./build/build.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist node_modules/.cache"
  },
  "dependencies": {
    "nokhwa-node": "^0.1.6",
    "rdev-node": "^1.0.1",
    "tray-icon-node": "^0.1.1",
    "webview-napi": "^0.1.8"
  }
}
